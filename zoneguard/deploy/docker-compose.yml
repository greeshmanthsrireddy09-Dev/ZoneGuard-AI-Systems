version: "3.9"

services:
  zoneguard-api:
    build:
      context: ..
      dockerfile: deploy/Dockerfile.backend
    container_name: zoneguard-api
    ports:
      - "8000:8000"
    volumes:
      - zoneguard_data:/app
    environment:
      - PYTHONUNBUFFERED=1

volumes:
  zoneguard_data:
